services:
  - type: web
    name: hvac-pipeline
    env: python
    plan: free
    buildCommand: cd app && pip install -r requirements.txt
    startCommand: cd app && gunicorn -w 4 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: GMAIL_APP_PASSWORD
        value: isqu gwkv hcin jrzz
      - key: PYTHON_VERSION
        value: 3.11.0
